// 0: Global stack slot

fun fib(n) {
    // stack 0: Call
    // stack 1: n

    if (n <= 2) {
        return n;
    }

    //     _   1
    //     _           _   2
    return fib(n -1) + fib(n -2);
}

print fib(2);
